[project]
name = "cogniwork-backend"
version = "0.0.1"
description = "CogniWork backend — FastAPI, Agent orchestration, Memory OS"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "pyyaml>=6.0",
    "uuid7>=0.1.0",  # 提供 uuid_extensions 模块
    "psycopg[binary,pool]>=3.2",
    "redis>=5.2",
    "PyJWT>=2.9",
    "argon2-cffi>=23.1",
    "email-validator>=2.2",
    "python-multipart>=0.0.12",
    "openpyxl>=3.1",
    "pypdf>=5.0",
    "python-docx>=1.1",
    "langgraph>=0.2",
    # RT-5：checkpointer 落 PostgreSQL。表结构属于 langgraph，跟着它的版本走，
    # 不抄进 apps/backend/migrations（见 main._open_checkpointer）。
    "langgraph-checkpoint-postgres>=2.0",
    "langchain-core>=0.3",
    "openai>=1.50",
    "anthropic>=0.40",
    "cryptography>=43.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cogniwork"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
# 发版检查项默认排除：开发期新增 Scope 时文案必然待审，
# 那时挡住合并没有意义。发版前用 `pytest -m release` 单独跑。
addopts = "-m 'not release'"
markers = [
    "release: 发版检查项，不阻塞 PR 合并（用 -m release 单独跑）",
    "integration: 需要 Postgres + Redis（CI 提供；本地无基础设施时自动 skip）",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query", "fastapi.File", "fastapi.Form"]

[tool.ruff.lint.per-file-ignores]
"tests/conftest.py" = ["E402"]
