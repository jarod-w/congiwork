# P0-08 桌面客户端与本地应用 Computer Use 设计文档

| 项 | 内容 |
|---|---|
| 优先级 | P0（Phase 1 必须交付，★★★★★，**独立高风险模块**） |
| 对应规划 | `ai_platform_plan.md` §2.3、§4.4、§6 Phase 1、§7.2 |
| 依赖 | `P0-07 隐私授权`、`P0-03 Agent Runtime`、`P0-04 工作台组件` |
| 被依赖 | `P1-01 Workflow Recorder`、`P1-05 白名单扩展` |
| 文档状态 | Draft |

---

## 1. 背景与目标

计划 §2.3 把这块定为「Phase 1 必须承担的已知成本」，且 §6 给了独立验收标准：**白名单应用操作成功率 ≥ 90%**。§231 行还提示要作为独立子团队/独立里程碑管理，避免拖累核心 SaaS 交付。

本文按此定位设计，并对计划中的技术方案提出一项**重要修正**（见 §4）。

目标：

- **G1** 桌面端提供 Web 端无法提供的能力：操作用户本机的应用与文件，对标竞品的深度系统集成。
- **G2** 白名单应用的操作成功率 ≥ 90%，且失败可解释、可恢复、可回滚。
- **G3** 权限颗粒度做到逐应用独立，且比 SaaS 工具更谨慎——因为侵入性更高。
- **G4** 桌面端不是第二个产品：账号、Memory、Skill 与 Web 完全共享（计划 §0.1）。

非目标（Phase 1）：

- 不做「通用操作任意本地应用」（计划 §0 明确用范围换风险）。
- 不做屏幕录制、键鼠原始流采集（这是 §4.5 明确排除的）。
- 不做 Desktop MCP（计划 §7.10 延后）。
- 不做 Linux 支持（Phase 1 仅 macOS + Windows）。

---

## 2. 需求拆分

| 编号 | 需求点 | 描述 | 验收 |
|---|---|---|---|
| DT-1 | Electron 壳与组件复用 | 复用 `packages/shared-ui`，桌面特有布局 | Web 组件改动自动同步 |
| DT-2 | 本地 Agent 进程 | Python 进程，负责应用自动化 | 崩溃自动重启，不影响 UI |
| DT-3 | 安全 IPC | Electron ↔ 本地 Agent 的受控通道 | 仅 loopback + 每次启动轮换 token |
| DT-4 | AppAdapter 抽象 | 统一的应用适配接口 | 新增应用不改 Agent 核心 |
| DT-5 | 首批适配 | Excel、邮件、浏览器（3 个 × 2 平台，优先级见 §2.1） | **每个（适配器 × 平台）组合** golden set 成功率 ≥90% |
| DT-6 | 逐应用授权 | 独立开关 + 系统权限引导 | 授权 Excel ≠ 授权邮件 |
| DT-7 | 执行可见与可控 | 执行前预览、执行中可中断、执行后可回滚 | 见 §7 |
| DT-8 | 稳定性策略 | 分层定位 + 重试 + 失败上报 | 见 §4 |
| DT-9 | 成功率度量 | golden task set + 每版回归 | 独立验收门禁 |
| DT-10 | 本地数据安全 | SQLite + SQLCipher，签名分发，自动更新 | 计划 §7.11 |
| DT-11 | 系统集成 | 托盘、通知、开机自启、全局快捷键 | |
| DT-12 | 观测与诊断 | 脱敏的失败上报，供适配维护 | 无用户内容外泄 |

### 2.1 平台范围与首批适配器复核（A1 / A2 / A4 / B3 确认后）

四个已确认结论都落在本模块，其中 A4 改动最大：

| 结论 | 对本模块的影响 |
|---|---|
| **A4 = 双平台** | Phase 1 同时交付 **Windows + macOS**。§8.3 的门禁粒度必须从「按适配器」细化为「按适配器 × 平台」，§13 工程量重估见下 |
| **A1 = 市场 / 运营** | 三个适配器全部命中该角色的高频任务，**首批清单不变**，但优先级重排（见下表） |
| **A2 = 海外优先** | 白名单锁定 Excel / Chrome-Edge / Outlook；**WPS、企微、飞书、钉钉明确不做**（原 §11 风险表已排除 WPS，此处升级为产品边界而非取舍） |
| ~~B3 = Gmail 暂不做~~ → **2026-08-18 改定：Gmail 回到首批** | **MailAdapter 的定位再次调整**（优先级不变、理由更换），见下 |

**适配器优先级重排**：

| 序 | 适配器 | 命中的 A1 高频任务 | 变化 |
|---|---|---|---|
| 1 | **Excel** | 数据整理 | 不变，仍是最复杂也最先做的 |
| 2 | **邮件** | 对外文案与邮件 | **从第三上调到第二**——原因见下 |
| 3 | **浏览器** | 数据整理（广告 / 内容 / 社媒后台无 API 或 API 门槛高） | 从第二下调到第三 |

**MailAdapter 排第二的理由（2026-08-18 改定后已更换，优先级不变）**：

| | B3 时期（Gmail 移出首批） | 改定后（Gmail 回到首批） |
|---|---|---|
| MailAdapter 的角色 | **Phase 1 唯一的真实邮件集成** | **唯一不依赖外部审核的邮件路径** |
| 不达标的后果 | 产品在「对外邮件」上只有粘贴降级 | Gmail 用户有连接器兜底；Outlook / M365 用户只有粘贴降级 |
| 排第二的理由 | 它是邮件能力的全部 | 它是 Google CASA 不确定性的对冲 |

结论相同但推理不同，这个区别有实际后果：**改定后 MailAdapter 的可砍程度上升了**。最坏情况下 Gmail 用户还有 SaaS 连接器可用（若 CASA 通过），所以 A9.1 的触发式预案里，「macOS 侧只上 Excel」这一刀比改定前更容易下——它砍掉的 macOS 邮件组合，现在有一条 Web 侧的替代路径。

`P0-05` §2.1.1 的 **G2 检查点（第 2 个月末）是本模块的关键外部输入**：G2 通过 → MailAdapter 的价值收敛为「覆盖 Outlook / M365」；G2 未通过且降级预案生效 → 读邮件的能力重新完全落回本模块，届时它的角色退回 B3 时期。**排期上不要在 G2 之前锁死 MailAdapter 的范围。**

**B5 的默认做法（「走 Graph API 而非操作桌面客户端」）仍然不成立**：Graph 只覆盖 Microsoft 365 账户，而 A1 目标用户里用 Gmail / Google Workspace 的比例很高。修正后的路径是——

| 用户的邮箱 | 路径 | 层 |
|---|---|---|
| Microsoft 365 / Outlook.com | **Graph API** | L1，最稳，首选 |
| Gmail / 其他 IMAP，Windows | **Outlook 桌面版 COM**（用户已在本机登录该账户） | L1，是原生对象模型而非 UI 自动化，符合 §4 分层策略 |
| Gmail / 其他 IMAP，macOS | **Mail.app AppleScript**（同上） | L1–L2 |

这条路径有个不容易注意到的好处：**它绕过了 Google OAuth 审核**。用户本机的邮件客户端已经登录了 Gmail 账户，通过 COM / AppleScript 操作它不需要我们向 Google 申请任何 scope。

**改定后这个好处不但没失效，反而变成了本模块最重要的性质**——它是 CASA 万一不通过时唯一能读 Gmail 的路径。代价是仅桌面端可用，且不是 API 级稳定。

**B5 已决（2026-08-18）：Gmail 优先走 SaaS 连接器；其余情况桌面端只做最简单的一条，即 Graph API。**

| 用户邮箱 | Phase 1 走哪条 | 由谁提供 |
|---|---|---|
| Gmail / Google Workspace | **Gmail 连接器** | `P0-05`（受 CASA 制约） |
| Microsoft 365 / Outlook.com | **Graph API** | 本模块 D6 |
| 其他（自建 IMAP 等） | 粘贴降级 | — |

**只做 Graph 的理由**（「哪个简单做哪个」的答案是确定的，不是随便挑）：它是纯 REST + 官方 SDK，不需要操作本地客户端进程、不依赖 Outlook 是否在运行、**一份实现同时覆盖 Windows 与 macOS**。另外两条各自只覆盖单平台且是脆弱的本地对象模型调用。**D6 因此从 10d 降到 4d**（§13）。

⚠️ **但这个选择去掉了一个对冲，必须写明白**：桌面 MailAdapter 原本是「CASA 万一不过」的兜底——Outlook COM 与 Mail.app 能操作用户在本机客户端里配置的**任意**账户，包括 Gmail。只做 Graph 之后这个兜底没有了，因为 **Graph 覆盖的恰好是那批本来就不需要 CASA 的用户（M365）**。两者的覆盖面不重叠，替代不了。

因此剩下一个明确的暴露面：

> **CASA 未过 × 用户邮箱是 Gmail** → 该用户在 Phase 1 **完全没有邮件能力，只剩粘贴**。

这个风险已被 A10 显式接受（退出条件不要求 Gmail 上线），但它的实际规模取决于目标用户的邮箱分布，而 A1 锚定的「20–300 人成长期公司的市场 / 运营」里 Google Workspace 占比通常不低。因此设一条**触发式补做**：

| 触发条件 | 动作 | 成本 |
|---|---|---|
| G2 检查点未过（`P0-05` §2.1.1）**且** `P0-07` §11 实验显示 Gmail 用户占比高 | 补做 **Outlook COM**（不是 Mail.app） | 3d |

补 Outlook COM 而非 Mail.app 的理由：Windows 装机量与「Gmail 账户配在桌面 Outlook 里」这个组合的覆盖面都更大，同样 3d 买到的用户更多。Mail.app 在 Phase 1 **不做**，它是 A9.1 预案第一刀本来就会砍掉的组合。

邮箱分布数据来自 `P0-07` §11 前置实验的「你的工作邮箱是 Gmail / Google Workspace 还是 Outlook / Microsoft 365」一题——**这题现在是上面那张触发表的唯一判据，不再只是排序参考**。

---

## 3. 架构

```text
┌─────────────────────────── 用户机器 ───────────────────────────┐
│                                                               │
│  ┌────────────────────────┐                                   │
│  │  Electron 主进程        │  窗口/托盘/通知/更新/权限引导       │
│  │   ├─ Renderer (React)  │  复用 shared-ui                    │
│  │   └─ AgentSupervisor   │  启动/监控/重启本地 Agent           │
│  └──────────┬─────────────┘                                   │
│             │ HTTP over loopback (127.0.0.1:随机端口)          │
│             │ + 每次启动轮换的 Bearer token                     │
│  ┌──────────▼─────────────┐                                   │
│  │  本地 Agent（Python）   │                                   │
│  │   ├─ AdapterRegistry   │                                   │
│  │   ├─ ExcelAdapter      │──▶ COM / AppleScript / openpyxl    │
│  │   ├─ BrowserAdapter    │──▶ CDP (Chrome DevTools Protocol)  │
│  │   ├─ MailAdapter       │──▶ Graph API / AppleScript         │
│  │   ├─ FallbackDriver    │──▶ Accessibility API → PyAutoGUI   │
│  │   └─ LocalStore        │──▶ SQLite + SQLCipher              │
│  └────────────────────────┘                                   │
└───────────────────────────────┬───────────────────────────────┘
                                │ HTTPS
                    ┌───────────▼────────────┐
                    │  后端（同一套 API）      │
                    │  Runtime / Memory /     │
                    │  Skill / Consent        │
                    └─────────────────────────┘
```

**执行权归属**：Agent Runtime 仍在**服务端**。桌面本地 Agent 是一个「远程 Executor」——服务端 Runtime 决定要调用 `desktop.excel.write_range`，通过桌面客户端的长连接下发指令，本地 Agent 执行后回传结果。

这样设计的理由：

1. 权限检查、审批、审计全部在服务端统一，不需要在本地重实现一遍（避免 `P0-07` 的无旁路保证出现缺口）。
2. 桌面端与 Web 端共享同一个 Task 视图，用户在手机/网页上也能看到桌面任务的进度和审批请求。
3. 本地 Agent 保持"哑执行器"定位，攻击面小、可测试性好。

代价是需要一条服务端 → 客户端的下行通道。用客户端主动建立的 SSE 长连接 + 指令拉取，避免入站端口。

---

## 4. 【重要修正】自动化技术方案：分层降级而非 PyAutoGUI 优先

计划 §4.4/§7.2 写的是「PyAutoGUI / Accessibility API」。**本文建议修正为分层降级策略，把 PyAutoGUI 降为最后兜底。**

### 4.1 修正理由

计划 §2.3 自己已经指出：「PyAutoGUI + Accessibility API 方案对目标应用 UI 变化敏感，维护成本高，跨平台行为差异大」。而 §6 又要求成功率 ≥90%。这两者在纯 UI 自动化路径上是矛盾的——基于坐标和控件树的自动化在真实办公环境（不同分辨率、不同 Office 版本、弹窗遮挡、系统主题）下很难稳定到 90%。

而这三个白名单应用**恰好都有远比 UI 自动化稳定的编程接口**。不用它们而去点 UI，是在自找计划里已经预警的那个麻烦。

### 4.2 分层定位策略

每个动作按以下顺序尝试，前一层不可用才降级：

| 层 | 手段 | 稳定性 | 适用 |
|---|---|---|---|
| **L1 原生 API** | Excel: COM (Win) / AppleScript+ScriptingBridge (mac) / openpyxl（文件态）<br>浏览器: CDP<br>邮件: Graph API / AppleScript | 高（不受 UI 变化影响） | **首选，覆盖目标 ≥85% 的动作** |
| **L2 Accessibility 树** | Win: UIAutomation<br>mac: AXUIElement | 中（受版本影响，但有语义） | L1 不覆盖的动作（如某些对话框） |
| **L3 视觉/坐标** | PyAutoGUI + 模板匹配 | 低 | 最后兜底，且**仅在 L1/L2 都失败时使用，并向用户明示"我要用比较笨的方式点界面，可能不准" ** |

L3 的每次使用都记入指标。**如果某个适配器 L3 使用率 > 10%，视为该适配器设计不合格**，需要重新用 L1/L2 实现——这是防止团队图省事全用 L3 的量化门禁。

### 4.3 各适配器的具体路径

| 应用 | 平台 | 主路径 | 说明 |
|---|---|---|---|
| **Excel** | Windows | COM (`win32com`) | 完整对象模型，读写单元格、公式、图表、透视表都稳定 |
| | macOS | **openpyxl（文件态）为主路径**；AppleScript 只用于轻动作 | 见下方说明 |
| | 两者 | openpyxl（文件态） | 应用未打开时直接改文件，最稳，优先采用 |
| **浏览器** | 两者 | CDP attach 到用户已开的 Chrome/Edge | 复用用户登录态；用 Playwright 的 `connect_over_cdp` |
| | | | 需用户以调试端口启动浏览器 → 由客户端引导并提供一键启动 |
| **邮件** | M365 账户 | Microsoft Graph | 首选，见 §2.1 的三行路径表 |
| | Windows / 非 M365 | Outlook 桌面版 COM | 覆盖 Gmail 等 IMAP 账户，绕过 Google OAuth 审核 |
| | macOS / 非 M365 | AppleScript (Mail.app) | 同上 |

**macOS 上 Excel 的主路径明确为 openpyxl 文件态**（A4 = 双平台确认后必须表态的一条）。原文写的是「AppleScript 为主、复杂操作降级为 openpyxl」，但既然两个平台都要过 ≥90% 门禁，这个顺序在 macOS 上撑不住——AppleScript 对 Excel 的对象模型覆盖远不如 COM，靠它做主路径就是拿 macOS 的成功率去赌。

修正后的 macOS 策略：

| 动作类型 | 路径 |
|---|---|
| 读写单元格 / 公式 / 排序 / 筛选 / 新建 sheet | **openpyxl 直接改文件**（应用可关闭，或改完提示重新打开） |
| 取当前选区、让 Excel 重新加载、另存为 | AppleScript（轻动作，稳定） |
| 透视表、图表等 openpyxl 覆盖不全的 | **明确标记为 macOS 不支持**，降级为「我给你改好的文件，你自己插图表」 |

代价是 macOS 上「Excel 正开着且有未保存修改」这个场景处理更麻烦（必须先让用户保存或放弃），要作为 golden set 的异常环境用例覆盖。收益是 macOS 的成功率不再依赖 AppleScript 的覆盖面。**明确标注不支持的动作，比用 L3 视觉兜底硬凑成功率好**——后者会同时踩穿 ≤10% 的 L3 门禁。

> 浏览器适配与 `P1-04` 浏览器自动化有重叠。边界：本模块负责「操作用户本机已打开的浏览器」（复用登录态），`P1-04` 负责「服务端托管浏览器执行任务」。两者共用一套动作原语，实现分开。

### 4.4 保留 PyAutoGUI 的场景

不是完全不用：系统级弹窗、文件选择对话框、应用未提供 API 的少数交互，仍需 L2/L3。因此 PyAutoGUI 仍在依赖中，只是不作为主路径。

---

## 5. AppAdapter 抽象（DT-4）

```python
class AppAdapter(ABC):
    app_id: str                    # 'excel'
    scope_key: str                 # 'desktop:excel:automate'
    platforms: set[str]            # {'darwin','win32'}

    @abstractmethod
    def probe(self) -> ProbeResult:
        """检测应用是否安装、版本、可用的接入层（L1/L2/L3）"""

    @abstractmethod
    def capabilities(self) -> list[ActionSpec]:
        """本适配器支持的动作，会被映射为 ToolSpec 暴露给 Runtime"""

    @abstractmethod
    def snapshot(self, ctx: Context) -> Snapshot:
        """执行前状态快照，用于预览与回滚"""

    @abstractmethod
    def execute(self, action: str, args: dict, ctx: Context) -> ActionResult:
        """执行动作，返回结果 + 实际使用的层级（L1/L2/L3）"""

    @abstractmethod
    def rollback(self, snapshot: Snapshot) -> bool:
        """尽力回滚；不可回滚的动作在 ActionSpec 中标记"""
```

`ActionSpec` 声明 `reversible: bool`，不可回滚的动作（发送邮件）在服务端映射为 `risk=irreversible`，走强制审批。

新增一个应用 = 实现一个 `AppAdapter` + 提供 golden task set，不改 Agent 核心。这是 `P1-05` 白名单扩展的基础。

---

## 6. 权限模型（DT-6）

三层权限，缺一不可：

```text
① 操作系统权限（macOS 辅助功能 / 自动化；Windows UIAccess）
      ↓ 由客户端引导用户在系统设置中授予，附带图文说明
② CogniWork Scope 授权（desktop:excel:automate）
      ↓ 走 P0-07 的四段式授权卡片，逐应用独立
③ 单次执行审批
      ↓ 走 P0-03 的审批链路
```

关键约束（对应计划 §2.3）：

- **逐应用独立**：`desktop:excel:automate` 与 `desktop:mail:automate` 是两个 Scope，两个开关，两次授权。
- **默认全部关闭**：安装桌面端不获得任何本地应用操作权限（计划 §5 明确）。
- **系统权限申请时机**：不在安装时申请，而在用户第一次开启某个应用授权时才引导。macOS 的「辅助功能」权限提示很吓人，提前申请会造成大量流失。
- **屏幕录制权限**：macOS 上 L3 视觉兜底需要「屏幕录制」权限。这个权限侵入性极高。**设计决策：Phase 1 不申请该权限，L3 兜底在 macOS 上退化为纯 Accessibility 定位。** 若某适配器确实需要，单独作为一个 Scope 申请并说明。

---

## 7. 执行可见与可控（DT-7，信任的关键）

```text
执行前                执行中                  执行后
─────────            ─────────              ─────────
状态快照              悬浮控制条              变更摘要
计划预览              · 当前动作              · 改了哪些单元格
"我将要…"             · [暂停] [停止]         · [撤销这次修改]
[开始] [取消]         · 每步高亮目标区域       · [查看详细记录]
```

要点：

1. **执行前预览必须具体**：不是「我要操作 Excel」，而是「我要在 Sheet1 的 D2:D50 填入计算后的转化率，覆盖现有内容」。这依赖 `snapshot()` 提供当前状态。
2. **执行中始终可中断**：屏幕上有一个常驻悬浮条，随时可停。这是用户敢开启的心理前提——AI 在操作我的电脑时，我必须能立刻叫停。
3. **执行后可回滚**：文件类操作在执行前复制一份到本地 SQLCipher 存储（保留 7 天），一键还原。不可回滚的动作提前标记并强制审批。
4. **鼠标接管提示**：L3 兜底会移动真实鼠标，执行前明确告知「接下来几秒我会控制鼠标，请不要操作」。

---

## 8. 成功率度量与验收（DT-9，独立验收门禁）

计划 §6 要求 ≥90% 成功率作为独立验收标准。需要可度量的定义。

### 8.1 Golden Task Set

每个适配器 **≥25 个任务用例**，覆盖：

| 类别 | 占比 | 例子（Excel） |
|---|---|---|
| 常规路径 | 60% | 读区域、写区域、加公式、排序、筛选、新建 sheet、透视表 |
| 边界情况 | 25% | 空表、10 万行大表、含合并单元格、含非 ASCII 列名、只读文件 |
| 异常环境 | 15% | 应用未启动、文件被占用、有未保存修改、弹窗遮挡 |

**双平台下的 golden set 规模（A4）**：同一套 ≥25 个用例在**两个平台各跑一遍**，并各自独立判定 ——「Windows 通过、macOS 不通过」是一个必须能被表达出来的结果，不能被平均成一个数字。此外每个平台增加平台特有用例：

| 平台 | 特有用例（每适配器 ≥3 个） |
|---|---|
| Windows | COM 注册异常 / Office 未激活 / 多个 Excel 实例同时打开 |
| macOS | 辅助功能权限未授予 / 自动化权限被拒 / 文件在 iCloud Drive 上未下载 |

即每适配器实际用例数 ≥ 25 × 2 + 6 ≈ **56**。三个适配器合计 ≈ 170 个用例，这是 §13 里 golden set 框架从 6d 涨到 10d 的主要原因。**用例本身尽量写成平台无关的声明式描述**（「在 D2:D50 写入转化率并读回校验」），由适配器层各自实现，否则用例数会真的翻倍而不是复用。

### 8.2 成功的定义

一次执行判定为成功，需同时满足：

1. 动作完成且无异常；
2. **结果校验通过**——不是"没报错就算成功"，而是执行后读回目标状态并与预期比对（写 D2:D50 后读回验证数值正确）；
3. 未产生预期外的副作用（快照比对，未改动其他区域）。

结果校验必须由适配器实现，是 `ActionResult` 的一部分。这条是 90% 这个数字有意义的前提。

### 8.3 回归门禁

| 项 | 要求 |
|---|---|
| 频率 | 每次 PR 跑受影响适配器（双平台）；每日全量跑一次 |
| 环境矩阵 | macOS 最新 + 前一版；Windows 11 + 10；Office 365 + Office 2021 |
| 门禁 | **（适配器 × 平台）成功率 < 90% → 阻止该组合进入发布白名单**（而非阻止该适配器、也不阻止整个发版） |
| L3 使用率 | > 10% → 告警并作为技术债登记，同样按平台分别统计 |
| 报告 | 结果写入 `docs/eval/desktop-adapters.md`，含每个用例的历史趋势，**按平台分列** |

「阻止该适配器进入白名单而非阻止整体发版」这个设计很重要：它让适配器可以按各自的成熟度独立上线，符合计划 §2.3「出问题时影响范围可控」的要求，也避免一个应用拖累整体交付。

**A4 确认双平台后，门禁粒度必须细化到（适配器 × 平台）**，这不是形式主义。如果按适配器合并统计，Windows 上 96% 与 macOS 上 82% 会平均成 89% 或 91%——无论落在哪一边，结论都是错的：前者冤枉了 Windows，后者让 macOS 用户拿到一个每五次坏一次的功能，而报表上写着「达标」。

因此白名单是一张矩阵，Phase 1 的合法交付结果可以长这样：

| | Windows | macOS |
|---|---|---|
| Excel | ✅ 上线 | ✅ 上线 |
| 邮件 | ✅ 上线 | ⛔ 未达标，不上线 |
| 浏览器 | ✅ 上线 | ✅ 上线 |

「macOS 上邮件功能暂不可用」是一个可以接受、可以对用户讲清楚的结果；「所有平台都 90% 左右但没人知道具体哪个组合在坏」不是。客户端 UI 必须能按平台展示当前可用的适配器，不能让用户点了才发现不支持。

---

## 9. 本地数据与分发安全（DT-10）

| 项 | 方案 |
|---|---|
| 本地存储 | SQLite + SQLCipher，密钥存 OS Keychain / Windows Credential Manager（计划 §7.11 Local First） |
| 存什么 | 待上传的执行记录、回滚快照、离线队列、设备级配置 |
| 不存什么 | **不缓存用户 Memory 全量、不缓存第三方凭据、不存屏幕截图（除非用户主动附加）** |
| 代码签名 | macOS: Developer ID + Notarization；Windows: EV code signing |
| 更新 | 自动更新（electron-updater），差分包，更新前校验签名 |
| 本地 Agent 完整性 | Electron 启动时校验 Agent 可执行文件签名，不匹配拒绝启动 |
| IPC 安全 | 仅监听 `127.0.0.1`，端口随机，Bearer token 每次启动轮换，token 仅通过进程启动参数传递 |
| 独立仓库 | 本地 Agent 单独仓库 + 独立签名与发布节奏（见 README「Repository Structure」） |

**IPC 是本模块最主要的本地攻击面**：本机上的任意进程如果能调用本地 Agent，就获得了操作用户 Excel/邮件的能力。除 loopback + 轮换 token 外，还需：请求带调用方进程校验（macOS 用 audit token，Windows 用 named pipe 的客户端 PID + 签名校验）。

---

## 10. 观测与诊断（DT-12）

适配器维护成本高（计划 §2.3），必须有数据支撑，否则只能靠用户投诉发现问题。

上报内容（**全部脱敏**，且受 `desktop:*:automate` Scope 覆盖，用户可关闭诊断上报）：

```json
{
  "adapter": "excel", "app_version": "16.89", "os": "darwin-14.5",
  "action": "write_range", "layer_used": "L1",
  "result": "failed", "error_class": "com_call_rejected",
  "duration_ms": 1840,
  "context_shape": {"range_cells": 49, "sheet_count": 3, "has_merged": true},
  "ui_tree_hash": "…"
}
```

- `context_shape` 只描述形状不含内容（49 个单元格，不含单元格里是什么）。
- `ui_tree_hash` 用于识别「应用更新导致控件树变化」——这是适配器批量失效的主要诱因，能提前预警。
- **绝不上报**：单元格内容、邮件正文、文件名、截图。

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 适配器脆弱性拖累 Phase 1 整体交付（计划 §231 明确预警） | 极高 | 独立里程碑 + 独立子团队；§8.3 的门禁按适配器粒度而非整体；**Phase 1 若只有 2 个适配器达标，就只上 2 个，不为凑数降低标准** |
| 纯 UI 自动化达不到 90% | 极高 | §4 分层策略，L1 原生 API 优先；L3 使用率作为质量指标 |
| macOS 权限申请造成流失 | 高 | 延迟到首次使用时申请；提供图文引导；不申请屏幕录制权限 |
| 本地 Agent 成为提权入口 | 极高 | §9 的 IPC 三重防护；本地 Agent 只执行白名单动作，不接受任意命令；安全评审作为发布前置 |
| 用户在 AI 操作时同时操作电脑，互相干扰 | 中 | L1 路径不受影响（不用鼠标）；L3 执行前明确提示并检测焦点变化，焦点丢失即中止 |
| Office 版本差异（365 vs 2021 vs WPS） | 高 | 环境矩阵回归；WPS **不在** Phase 1 白名单内——A2 = 海外优先后，这是产品边界而非排期取舍 |
| **双平台把本模块推到 107 人日，占 P0 总量约 35%**（A4 的直接代价；B5 只做 Graph 后从 113 降下来） | 极高 | A9 已定案：Phase 1 周期放到 3.5 个月，**本模块范围不砍**，三个适配器 × 双平台全做。<br>但余量极薄（全项目约 2 人日），因此保留触发式预案：**第 1 个月末容量复核落后 > 10% 时，砍 macOS 的邮件与浏览器组合（**原估 18d，B5 已决后重算约 15d**——Mail.app 本就不做，这一刀不再包含它），保 Excel 双平台**——Excel 是 A1 高频任务里最刚性的一个。§8.3 的矩阵粒度门禁让这么砍不需要改任何设计，但**必须在第一个月末决定**，拖到后期已投入的 macOS 适配就全废了 |
| CDP attach 需要用户以调试模式启动浏览器，体验差 | 中 | 客户端提供「用 CogniWork 打开浏览器」的一键入口（带调试端口启动）；不支持时降级为独立浏览器实例（走 `P1-04`） |
| 桌面端与 Web 端体验割裂 | 中 | 共享 `shared-ui`；桌面端不做独立的 UI 演进路线 |

---

## 12. 验收标准

1. **每个（适配器 × 平台）组合的 golden set 成功率 ≥ 90%**（含结果校验，见 §8.2），在该平台的环境矩阵全部组合上达标。达标的组合进入白名单，未达标的组合不上线且在客户端明示不可用——**不做跨平台平均**。
2. L3（视觉/坐标）使用率 ≤ 10%，按平台分别统计。
3. 未开启任何 `desktop:*` Scope 的客户端，本地 Agent 不启动（进程不存在）。
4. 授权 Excel 后，尝试调用邮件适配器返回 `permission_denied` 且无任何邮件客户端进程交互。
5. 执行中点击「停止」，2 秒内实际停止；已完成的部分可回滚。
6. IPC 安全：从另一个未授权进程调用本地 Agent 端口，全部请求被拒。
7. 诊断上报中无任何用户内容（用真实任务跑一轮，人工审查全部上报字段）。
8. 桌面端登录后，Web 端创建的 Memory 与 Skill 立即可用（共享验证）。

---

## 13. 交付拆分（独立里程碑管理）

**A4 = 双平台确认后已重估。** 下表两列并列，便于看清双平台的增量落在哪里：

| 里程碑 | 内容 | 单平台原估 | **双平台重估** | 增量来自 |
|---|---|---|---|---|
| D0 | 技术预研：三个应用的 L1 接口可行性验证 | 5d | **8d** | 两平台各验一遍；含 macOS 上 openpyxl 对常见操作的覆盖面盘点 |
| D1 | Electron 壳 + shared-ui 复用 + 登录态打通 | 5d | **6d** | 两平台窗口/托盘行为差异 |
| D2 | 本地 Agent 骨架 + 安全 IPC + Supervisor | 5d | **7d** | 调用方进程校验两套实现：macOS audit token / Windows named pipe |
| D3 | AppAdapter 抽象 + Runtime 远程 Executor 通道 | 5d | 5d | 平台无关，无增量 |
| D4 | ExcelAdapter | 10d | **16d** | Win COM 8d + macOS openpyxl / AppleScript 8d |
| D5 | golden set 框架 + 结果校验 + CI 环境矩阵 | 6d | **10d** | 双平台 CI runner + 用例数从 ~25 涨到 ~56（§8.1） |
| D6 | **MailAdapter**（B5 已决：只做 Graph） | 6d | **4d** | ~~Graph 4d + Outlook COM 3d + Mail.app 3d~~ → **Graph 4d，一份实现跨双平台**。Outlook COM 转为触发式补做（§2.1），Mail.app 不做 |
| D7 | BrowserAdapter（CDP） | 6d | **8d** | CDP 本身跨平台，增量在浏览器发现与调试端口启动引导 |
| D8 | 逐应用授权 UI + 系统权限引导 | 5d | **8d** | macOS 辅助功能 / 自动化 + Windows UIAccess 两套图文引导 |
| D9 | 执行预览/中断/回滚 | 6d | **7d** | 悬浮控制条的两平台实现 |
| D10 | 本地加密存储 + 签名分发 + 自动更新 | 6d | **9d** | Developer ID + Notarization 与 EV 签名两条发布通道 |
| D11 | 观测上报 + 诊断 | 3d | **4d** | 上报字段增加平台维度 |
| **小计** | | 68d | **92d** | B5 已决后 98d − 6d |
| **缓冲** | **专门预留**（计划 §2.3 要求） | **10d** | **15d** | 双平台的不确定性更高；仍**不得挪用** |
| **合计** | | **78d** | **107d** | 原 113d，B5「只做 Graph」省 6d |

三条必须说清的账：

1. **口径修正**：`P0-open-questions.md` §0.1 把本模块记为「68 人日（含 10d 缓冲）」，但原 §13 的里程碑本身就加总到 68d，缓冲 10d 是额外的。即单平台的真实数字是 **78d**，原表少算了 10d。双平台重估为 **113d**。
2. **P0 总量随之变化**：其余 7 个模块 + 本模块。截至 2026-08-18 的最新口径是 **约 302 人日**——A4 双平台把本模块推到 113d，`P0-05` 连接器改定 +5d，B5「只做 Graph」−6d。见 `P0-open-questions.md` §0.1 与 A10。
3. **B5 的 −6d 不是省出来的余量，是范围变小了**：Outlook COM 与 Mail.app 的用户仍然存在，只是 Phase 1 让他们走粘贴降级。§2.1 的触发表说明了什么条件下要把其中一条补回来（+3d）。
4. **接受矩阵式交付**：§8.3 的门禁是（适配器 × 平台）粒度，因此「Phase 1 只上线达标的组合」不仅是可接受结果，而是**预期结果**。不要通过降低标准去凑满六个格子。

---

## 14. 待决问题

1. ~~是否先只在 Windows 上提供 Excel 适配~~ → **已决（A4 = 双平台）**：Windows 与 macOS 同时交付。原本担心的「macOS AppleScript 能力不足」用**换主路径**解决而不是降标准——macOS 改以 openpyxl 文件态为主，覆盖不到的动作明确标注不支持（§4.3）。D0 预研仍是 Go/No-Go 检查点，但检查的对象变了：现在要验的是「openpyxl 能覆盖多少常见 Excel 操作」。
2. ~~邮件客户端选型~~ → **已决（B5，2026-08-18）**：**Gmail 用户走 SaaS 连接器；其余走 Graph API，桌面端 Phase 1 只做这一条。** Outlook COM 转为触发式补做（条件见 §2.1），Mail.app 不做。D6 从 10d 降到 4d。
   - **遗留的暴露面已登记在 §2.1**：CASA 未过 × Gmail 用户 = 无邮件能力。该风险由 A10 显式接受，但其规模需 `P0-07` §11 实验的邮箱分布数据来确认，那题现在是触发表的判据。
3. ~~「本地 Agent 独立仓库」的边界：共享的 proto/schema 放在哪？~~ → **已决（B11，2026-08-18 确认默认做法）**：**发布为版本化的包，由两边各自依赖。**
   - 包本身放在主仓库 `packages/` 下发布，桌面 Agent 仓库按版本号依赖它。不用 git submodule，也不复制粘贴。
   - **两边各自依赖 = 两边版本必然会漂移**，这是这个方案的固有代价，不是可以靠纪律避免的：桌面 Agent 有独立的签名与分发节奏（README「Repository Structure」），用户机器上的版本落后于后端是常态而非异常。因此必须配套两条：
     1. **schema 演进只允许向后兼容的变更**（加可选字段可以，改字段语义、删字段、改必填性不可以）。破坏性变更必须走新的消息类型，让旧客户端明确失败而不是静默误解。
     2. **握手时交换版本号**，后端对过旧的 Agent 明确拒绝并提示升级，而不是尝试兼容。**明确拒绝比勉强兼容安全**——桌面端执行的是有副作用的操作，一次因 schema 漂移导致的误解可能是不可撤销的动作。
   - 版本号与兼容窗口的具体策略留给实现时定，但上面两条是设计约束，不是实现细节。
4. ~~双平台后的团队配置~~ → **已决（A9）**：Phase 1 周期放到 3.5 个月，5 人不变，**本模块范围不砍**。砍 macOS 组合降级为触发式预案，触发条件与时点见 §11 风险表末行。
