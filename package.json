{
  "name": "cogniwork",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev:web": "pnpm --filter @cogniwork/web dev",
    "build:web": "pnpm --filter @cogniwork/web build",
    "test:backend": "cd apps/backend && .venv/bin/python -m pytest -q",
    "lint:backend": "cd apps/backend && .venv/bin/python -m ruff check ."
  }
}
